---
phase: 02-text-processing-entity-linking
plan: 01
type: execute
depends_on: ["01-04"]
files_modified: [pyproject.toml, src/imst_quant/processing/__init__.py, src/imst_quant/processing/language.py, src/imst_quant/processing/deduplication.py, src/imst_quant/processing/normalizer.py]
domain: python-nlp
---

<objective>
Implement text processing pipeline: language detection, MinHash deduplication, and text normalization.

Purpose: Fulfill ING-07 (MinHash dedup), ING-08 (non-English filter). Filter non-English posts (>95% accuracy), remove near-duplicates, normalize text while preserving raw.

Output: LanguageDetector, Deduplicator, TextNormalizer modules; add fasttext, datasketch to dependencies.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRD.md (sections 4.2, 4.3)

**PRD patterns:** fasttext lid.176.ftz for language; datasketch MinHash/MinHashLSH; threshold 0.8 for both
**Tech stack:** fasttext-wheel>=0.9.2, datasketch>=1.6.4 per PRD_PART5
**Input:** Bronze Reddit parquet (title + selftext concatenated)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and implement LanguageDetector</name>
  <files>pyproject.toml, src/imst_quant/processing/__init__.py, src/imst_quant/processing/language.py</files>
  <action>
Add to pyproject.toml dependencies: fasttext-wheel>=0.9.2, datasketch>=1.6.4, emoji>=2.0.

Create src/imst_quant/processing/ with __init__.py.

Create language.py: LanguageDetector class per PRD FR-TXT-01. Use fasttext.load_model(model_path). Default model_path: data/models/lid.176.bin. On first use, if file missing: download from https://dl.fbaipublicfiles.com/fasttext/supervised-models/lid.176.bin to model_path (mkdir -p parent). Method is_english(text: str) -> Tuple[bool, float]: predict lang+confidence, return (lang=="en" and conf>=0.8), conf. Replace newlines with space before prediction. Handle empty/short text (return False, 0.0).
  </action>
  <verify>python -c "
from imst_quant.processing.language import LanguageDetector
d = LanguageDetector()
ok, conf = d.is_english('Apple stock is going up')
assert ok and conf > 0.5
ok2, _ = d.is_english('Esta es una frase en español')
assert not ok2
print('ok')
" succeeds</verify>
  <done>LanguageDetector filters non-English with configurable threshold</done>
</task>

<task type="auto">
  <name>Task 2: Implement Deduplicator with MinHash</name>
  <files>src/imst_quant/processing/deduplication.py</files>
  <action>
Create deduplication.py: Deduplicator class per PRD FR-TXT-02. Use datasketch MinHash, MinHashLSH. __init__(num_perm=128, threshold=0.8). get_minhash(text) -> MinHash: tokenize on whitespace, lower, update each word.encode('utf8'). is_duplicate(post_id, text) -> Tuple[bool, Optional[str]]: compute MinHash, query LSH; if match return (True, first_dup_id); else insert post_id and return (False, None). Use MinHashLSH(threshold=threshold, num_perm=num_perm).
  </action>
  <verify>python -c "
from imst_quant.processing.deduplication import Deduplicator
d = Deduplicator()
dup1, _ = d.is_duplicate('1', 'Apple AAPL stock going up')
dup2, _ = d.is_duplicate('2', 'Apple AAPL stock going up')
assert not dup1 and dup2
print('ok')
" succeeds</verify>
  <done>Deduplicator removes near-duplicates via MinHash LSH</done>
</task>

<task type="auto">
  <name>Task 3: Implement TextNormalizer</name>
  <files>src/imst_quant/processing/normalizer.py</files>
  <action>
Create normalizer.py: TextNormalizer and NormalizedText NamedTuple per PRD FR-TXT-03. normalize(text) -> NormalizedText: raw (original), cleaned (normalized), urls_removed (count), emojis (list). Remove URLs (https?://, www.), replace with [URL]; standardize $TICKER to uppercase; collapse whitespace. Use regex patterns from PRD. Optional: use emoji package for _extract_emojis. Preserve raw text in output.
  </action>
  <verify>python -c "
from imst_quant.processing.normalizer import TextNormalizer, NormalizedText
n = TextNormalizer()
r = n.normalize('Check out $aapl https://x.com/abc  great stock')
assert r.raw == 'Check out $aapl https://x.com/abc  great stock'
assert 'AAPL' in r.cleaned or 'aapl' in r.cleaned.upper()
assert r.urls_removed == 1
print('ok')
" succeeds</verify>
  <done>TextNormalizer outputs raw + cleaned; URLs removed; cashtags uppercased</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pip install -e . installs fasttext, datasketch
- [ ] LanguageDetector achieves >95% on sample (manual spot-check English vs Spanish)
- [ ] Deduplicator detects near-duplicate text
- [ ] TextNormalizer preserves raw, outputs cleaned
</verification>

<success_criteria>

- Language detection filters non-English (ING-08)
- MinHash deduplication working (ING-07)
- Text normalization preserves raw, outputs cleaned
- All modules importable and tested
  </success_criteria>

<output>
After completion, create `.planning/phases/02-text-processing-entity-linking/02-01-SUMMARY.md` per summary template.
</output>
