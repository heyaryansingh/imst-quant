---
phase: 02-text-processing-entity-linking
plan: 02
type: execute
depends_on: ["01-04"]
files_modified: [pyproject.toml, src/imst_quant/entities/__init__.py, src/imst_quant/entities/cashtag.py, src/imst_quant/entities/aliases.py, src/imst_quant/entities/disambiguator.py, config/ticker_aliases.yaml]
domain: python-nlp
---

<objective>
Implement entity linking: cashtag extraction, ticker/alias dictionary, and contextual disambiguation.

Purpose: Fulfill ENT-01, ENT-02, ENT-03. Extract $TICKER patterns, map aliases to tickers, disambiguate via embeddings. Prepare for ENT-04 (>90% precision validation in 02-03).

Output: Cashtag extractor, ticker/alias config, EntityDisambiguator, EntityLink dataclass.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRD.md (section 4.2 - FR-ENT-01, FR-ENT-02)

**PRD patterns:** CASHTAG_PATTERN r'\$([A-Z]{1,5})\b'; TICKER_DICT, COMPANY_ALIASES, CRYPTO_ALIASES; SentenceTransformer all-MiniLM-L6-v2
**Paper stocks:** AAPL, JNJ, JPM, XOM - ensure in ticker dict
**Tech stack:** sentence-transformers>=2.2.2
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement cashtag extraction and ticker alias config</name>
  <files>src/imst_quant/entities/__init__.py, src/imst_quant/entities/cashtag.py, src/imst_quant/entities/aliases.py, config/ticker_aliases.yaml</files>
  <action>
Add sentence-transformers>=2.2.2 to pyproject.toml.

Create src/imst_quant/entities/ with __init__.py.

Create cashtag.py: extract_cashtags(text: str) -> List[str] per PRD FR-ENT-01. Regex r'\$([A-Za-z]{1,5})\b', return [m.upper() for m in matches]. Handle edge cases (empty text, no cashtags).

Create config/ticker_aliases.yaml with structure:
- tickers: {AAPL: {name, sector}, JNJ, JPM, XOM, ...} - paper stocks + common
- company_aliases: {apple: [AAPL], nvidia: [NVDA], ...}
- crypto_aliases: {bitcoin: [BTC], btc: [BTC], ...}

Create aliases.py: load_aliases() -> (ticker_dict, company_aliases, crypto_aliases) from YAML. Include paper stocks AAPL, JNJ, JPM, XOM.
  </action>
  <verify>python -c "
from imst_quant.entities.cashtag import extract_cashtags
assert extract_cashtags('\$AAPL and \$aapl') == ['AAPL', 'AAPL']
from imst_quant.entities.aliases import load_aliases
td, ca, cra = load_aliases()
assert 'AAPL' in td and 'JNJ' in td
print('ok')
" succeeds</verify>
  <done>Cashtag extraction works; alias config loads</done>
</task>

<task type="auto">
  <name>Task 2: Implement EntityDisambiguator with embeddings</name>
  <files>src/imst_quant/entities/disambiguator.py</files>
  <action>
Create disambiguator.py per PRD FR-ENT-02. EntityLink dataclass: asset_id, confidence, method, matched_span.

EntityDisambiguator:
- __init__(model_name='all-MiniLM-L6-v2', confidence_threshold=0.7): load SentenceTransformer, precompute asset embeddings from ticker_dict (desc = f"{ticker} {info['name']} {info['sector']}")
- disambiguate(text, candidates, subreddit, confidence_threshold) -> List[EntityLink]: encode text, cosine sim vs each candidate; subreddit prior: if subreddit in crypto_subs boost crypto candidates; return links where sim >= threshold
- _subreddit_matches(subreddit, ticker): crypto_subs = {cryptocurrency, bitcoin, ethereum, cryptomarkets}; if subreddit in crypto_subs, ticker must be in crypto set

Use numpy for cosine similarity. Lazy-load model on first disambiguate call.
  </action>
  <verify>python -c "
from imst_quant.entities.disambiguator import EntityDisambiguator, EntityLink
from imst_quant.entities.aliases import load_aliases
td, ca, cra = load_aliases()
d = EntityDisambiguator()
links = d.disambiguate('Apple stock is great', ['AAPL'], 'stocks')
assert len(links) >= 1 and links[0].asset_id == 'AAPL'
print('ok')
" succeeds</verify>
  <done>EntityDisambiguator produces EntityLinks with confidence</done>
</task>

<task type="auto">
  <name>Task 3: Implement entity linker pipeline</name>
  <files>src/imst_quant/entities/linker.py</files>
  <action>
Create linker.py: EntityLinker class that combines extraction + aliases + disambiguation.

link_entities(text: str, subreddit: str) -> List[EntityLink]:
1. Extract cashtags via extract_cashtags
2. Resolve aliases: for each word/token in normalized text, check company_aliases and crypto_aliases; collect candidate tickers
3. Merge candidates (cashtags + alias matches), dedupe
4. Call disambiguator.disambiguate(text, candidates, subreddit)
5. Return combined EntityLinks (dedupe by asset_id, keep highest confidence)

Handle empty text, no candidates. Use configurable confidence threshold.
  </action>
  <verify>python -c "
from imst_quant.entities.linker import EntityLinker
linker = EntityLinker()
links = linker.link_entities('Apple AAPL and Microsoft are both up', 'stocks')
tickers = {l.asset_id for l in links}
assert 'AAPL' in tickers or 'MSFT' in tickers
print('ok')
" succeeds</verify>
  <done>EntityLinker produces entity links from raw text</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] extract_cashtags handles $TICKER patterns
- [ ] Alias config includes paper stocks
- [ ] EntityDisambiguator uses SentenceTransformer
- [ ] EntityLinker combines extraction and disambiguation
</verification>

<success_criteria>

- Cashtag extraction (ENT-01)
- Alias mapping (ENT-02)
- Contextual disambiguation (ENT-03)
- EntityLinker pipeline functional
  </success_criteria>

<output>
After completion, create `.planning/phases/02-text-processing-entity-linking/02-02-SUMMARY.md` per summary template.
</output>
