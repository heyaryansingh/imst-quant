---
phase: 03-baseline-sentiment
plan: 01
type: execute
depends_on: [02-03]
files_modified: [pyproject.toml, src/imst_quant/sentiment/__init__.py, src/imst_quant/sentiment/textblob.py, src/imst_quant/sentiment/aggregation.py, scripts/silver_to_sentiment.py, tests/test_sentiment.py]
domain: python-nlp

---

<objective>
Implement baseline sentiment (SENT-01 to SENT-04): TextBlob polarity, 3-hour bucketing, daily aggregation with influence weighting.
</objective>

<context>
@.planning/PRD_PART2.md FR-SENT-01, FR-SENT-02
Silver posts have entity_links JSON. Influence scores from Phase 4; use 1.0 default until then.
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaselineSentimentAnalyzer</name>
  <files>src/imst_quant/sentiment/textblob.py</files>
  <action>
Add textblob to pyproject.toml.
Create sentiment/textblob.py: SentimentScore(NamedTuple) polarity, subjectivity, method.
BaselineSentimentAnalyzer.analyze(text) -> SentimentScore using TextBlob.
</action>
  <verify>python -c "from imst_quant.sentiment.textblob import BaselineSentimentAnalyzer; a=BaselineSentimentAnalyzer(); s=a.analyze('Apple is great'); assert -1<=s.polarity<=1; print('ok')"</verify>
</task>

<task type="auto">
  <name>Task 2: Time bucketing and daily aggregation</name>
  <files>src/imst_quant/sentiment/aggregation.py</files>
  <action>
BUCKET_HOURS=3, BUCKETS_PER_DAY=8.
assign_time_bucket(timestamp) -> bucket 0-7.
aggregate_daily_sentiment(posts_df, date, influence_scores) -> Dict[asset_id, sentiment_index].
Formula: sum(polarity_i * influence_i) / sum(influence_i). Default influence 1.0.
posts_df must have: asset_id, author_id, polarity, date.
</action>
  <verify>python -c "from imst_quant.sentiment.aggregation import assign_time_bucket, aggregate_daily_sentiment; from datetime import datetime; assert assign_time_bucket(datetime(2024,1,15,6,0))==2; print('ok')"</verify>
</task>

<task type="auto">
  <name>Task 3: Silver to sentiment pipeline</name>
  <files>scripts/silver_to_sentiment.py, src/imst_quant/sentiment/pipeline.py</files>
  <action>
Create pipeline: read silver posts -> expand entity_links -> TextBlob polarity -> write sentiment_aggregates.parquet (date, asset_id, sentiment_index, post_count).
Support influence_scores dict (default 1.0). Output: data/sentiment/sentiment_aggregates.parquet or gold layer.
</action>
  <verify>Import pipeline; with silver data, produces sentiment aggregates</verify>
</task>

<task type="auto">
  <name>Task 4: Unit tests</name>
  <files>tests/test_sentiment.py</files>
  <action>
Test: BaselineSentimentAnalyzer polarity range.
Test: assign_time_bucket buckets.
Test: aggregate_daily_sentiment formula (mock posts).
</action>
  <verify>pytest tests/test_sentiment.py -v passes</verify>
</task>

</tasks>

<success_criteria>
- SENT-01: TextBlob polarity
- SENT-02: 3-hour bucketing
- SENT-03: Influence weighting (default 1.0)
- SENT-04: Daily sentiment index per asset
</success_criteria>
