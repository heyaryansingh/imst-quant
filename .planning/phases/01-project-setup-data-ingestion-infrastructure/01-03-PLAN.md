---
phase: 01-project-setup-data-ingestion-infrastructure
plan: 03
type: execute
depends_on: ["01-01"]
files_modified: [src/imst_quant/ingestion/market.py, src/imst_quant/ingestion/crypto.py, scripts/ingest_market.py]
domain: python-data-engineering
---

<objective>
Implement equity and crypto OHLCV ingestion via yfinance and CCXT with causality-preserving raw storage.

Purpose: Fulfill ING-04, ING-05. Fetch daily OHLCV for paper stocks (AAPL, JNJ, JPM, XOM) and crypto pairs (BTC/USDT, ETH/USDT), store with retrieved_at, use auto_adjust for equities, enableRateLimit for CCXT.

Output: Equity and crypto ingestion modules, raw JSON storage, runnable market ingestion script.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-data-ingestion-infrastructure/01-RESEARCH.md
@.planning/phases/01-project-setup-data-ingestion-infrastructure/01-01-SUMMARY.md

**From RESEARCH - DO NOT hand-roll:** yfinance auto_adjust=True for splits; CCXT enableRateLimit=True; use Polars for DataFrame ops
**Paper stocks (ROADMAP):** AAPL, JNJ, JPM, XOM
**Crypto:** CCXT unified API, binance default exchange
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement equity OHLCV ingestion with yfinance</name>
  <files>src/imst_quant/ingestion/market.py</files>
  <action>
Create market.py with ingest_equity_ohlcv(tickers: list[str], start_date: str, end_date: str, output_dir: Path) -> pl.DataFrame.

Use yf.download(tickers, start, end, interval="1d", group_by="ticker", auto_adjust=True, threads=True). Add retrieved_at = datetime.now(timezone.utc).isoformat() to each record. Output schema: ticker, date (YYYY-MM-DD), open, high, low, close, volume, retrieved_at. Handle multi-ticker vs single-ticker yfinance response shape (group_by="ticker" returns different column structure). Use Polars for DataFrame. Handle pandas DataFrame from yfinance, convert to Polars. Save raw JSON to output_dir / "market" / f"equity_{start_date}_{end_date}.json" per RESEARCH.
  </action>
  <verify>python -c "
from imst_quant.ingestion.market import ingest_equity_ohlcv
from pathlib import Path
import polars as pl
df = ingest_equity_ohlcv(['AAPL'], '2024-01-01', '2024-01-10', Path('data/raw'))
assert 'retrieved_at' in df.columns
assert len(df) > 0
print('ok')
" succeeds</verify>
  <done>Equity OHLCV fetched with retrieved_at; raw JSON saved; Polars DataFrame returned</done>
</task>

<task type="auto">
  <name>Task 2: Implement crypto OHLCV ingestion with CCXT</name>
  <files>src/imst_quant/ingestion/crypto.py</files>
  <action>
Create crypto.py with ingest_crypto_ohlcv(exchange_id: str, symbols: list[str], timeframe: str="1d", since_days: int=365, output_dir: Path = None) -> pl.DataFrame.

Initialize exchange with {"enableRateLimit": True}. Use exchange.fetch_ohlcv(symbol, timeframe, since=..., limit=1000). Schema: exchange, symbol, timestamp, date (YYYY-MM-DD), open, high, low, close, volume, retrieved_at. Handle ccxt.BaseError per symbol (log and skip, don't crash). Check exchange.has.get("fetchOHLCV") before calling. Save raw to output_dir / "crypto" / f"crypto_{exchange_id}_{since_days}d.json" if output_dir. Use tenacity for retry on transient failures.
  </action>
  <verify>python -c "
from imst_quant.ingestion.crypto import ingest_crypto_ohlcv
from pathlib import Path
df = ingest_crypto_ohlcv('binance', ['BTC/USDT'], since_days=7, output_dir=Path('data/raw'))
assert 'retrieved_at' in df.columns
assert len(df) > 0
print('ok')
" succeeds</verify>
  <done>Crypto OHLCV fetched with CCXT; retrieved_at present; rate limiting enabled</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI script for market data ingestion</name>
  <files>scripts/ingest_market.py</files>
  <action>
Create scripts/ingest_market.py that:
1. Loads Settings for equity_tickers (default AAPL,JNJ,JPM,XOM) and crypto_pairs (default BTC/USDT, ETH/USDT)
2. Computes start_date/end_date (default: last 90 days)
3. Calls ingest_equity_ohlcv, saves to data/raw/market/
4. Calls ingest_crypto_ohlcv for binance, saves to data/raw/crypto/
5. Accepts --start, --end, --equity-only, --crypto-only for flexibility

Use argparse. Document usage in docstring. No API keys required for yfinance or CCXT public endpoints.
  </action>
  <verify>python scripts/ingest_market.py --help succeeds; python scripts/ingest_market.py runs and produces data/raw/market/*.json and data/raw/crypto/*.json</verify>
  <done>Market ingestion script runs; OHLCV for 4 paper stocks + crypto saved to data/raw/</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ingest_equity_ohlcv returns data for AAPL, JNJ, JPM, XOM
- [ ] ingest_crypto_ohlcv uses enableRateLimit=True
- [ ] All records have retrieved_at
- [ ] Raw JSON in data/raw/market/ and data/raw/crypto/
</verification>

<success_criteria>

- Market data ingestion fetches OHLCV for 4 paper stocks (ING-04)
- Crypto OHLCV fetched for configured pairs (ING-05)
- Raw data stored with correct timestamps
- Script runs without API keys (yfinance/CCXT public)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-data-ingestion-infrastructure/01-03-SUMMARY.md` per summary template.
</output>
