---
phase: 01-project-setup-data-ingestion-infrastructure
plan: 01
type: execute
depends_on: []
files_modified: [pyproject.toml, .env.example, .gitignore, config/subreddits.yaml, src/imst_quant/config/settings.py, src/imst_quant/__init__.py, src/imst_quant/config/__init__.py]
domain: python-data-engineering
---

<objective>
Establish project foundation: repository structure, Python packaging, and type-safe configuration.

Purpose: Create the scaffold that all ingestion and storage code will use. Matches ING-01 through ING-06 prerequisites and RESEARCH.md standard stack.

Output: src/ layout with imst_quant package, pyproject.toml with dependencies, Pydantic Settings, .env.example, config/subreddits.yaml, and data directory structure.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-data-ingestion-infrastructure/01-RESEARCH.md

**Tech stack from RESEARCH:** PRAW, yfinance, CCXT, Polars, PyArrow, Pydantic Settings, structlog, tenacity
**Established patterns:** src/ layout, Pydantic Settings for config, env_prefix for nested settings
**Paper stocks (PRD_PART6):** AAPL, JNJ, JPM, XOM
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repository structure and pyproject.toml</name>
  <files>pyproject.toml, src/imst_quant/__init__.py, src/imst_quant/config/__init__.py, src/imst_quant/ingestion/__init__.py, src/imst_quant/storage/__init__.py, src/imst_quant/utils/__init__.py, tests/unit/__init__.py, tests/integration/__init__.py</files>
  <action>
Create directory structure per RESEARCH.md:
- src/imst_quant/{config,ingestion,storage,utils}/
- tests/{unit,integration}/
- data/raw/{reddit,market,crypto}/
- data/bronze/{reddit,market,crypto}/
- config/

Create pyproject.toml with:
- [build-system] setuptools-scm or hatchling
- [project] name=imst-quant, python>=3.11, dependencies: praw>=7.7.1, yfinance>=0.2.40, ccxt>=4.4, polars>=1.0, pyarrow>=17.0, pydantic-settings>=2.13, structlog>=24.0, tenacity>=8.0
- [project.optional-dependencies] dev: pytest, pytest-cov
- [tool.pytest.ini_options] testpaths = ["tests"]
Add empty __init__.py files. Create .gitignore with .env, __pycache__/, .venv/, data/raw/, data/bronze/, *.parquet. Use pathlib Path for data dirs. Do NOT add a virtual env to the repo.
  </action>
  <verify>python -c "import imst_quant; print('ok')" succeeds from project root after pip install -e .</verify>
  <done>pyproject.toml installs package; src layout exists; data dirs exist</done>
</task>

<task type="auto">
  <name>Task 2: Implement Pydantic Settings and .env.example</name>
  <files>src/imst_quant/config/settings.py, .env.example</files>
  <action>
Create settings.py with Pydantic Settings classes per RESEARCH.md Pattern 4:
- RedditSettings: REDDIT_CLIENT_ID, REDDIT_CLIENT_SECRET (SecretStr), REDDIT_USER_AGENT (default "IMST-Quant/1.0")
- DataSettings: DATA_RAW_DIR (Path default "data/raw"), DATA_BRONZE_DIR (Path default "data/bronze")
- MarketSettings: equity_tickers list default ["AAPL","JNJ","JPM","XOM"], crypto_pairs default ["BTC/USDT","ETH/USDT"]
- Root Settings that composes reddit, data, market

Use SettingsConfigDict(env_prefix, env_file=".env"). Create .env.example with REDDIT_CLIENT_ID=, REDDIT_CLIENT_SECRET=, REDDIT_USER_AGENT=IMST-Quant/1.0. Never hardcode secrets.
  </action>
  <verify>python -c "from imst_quant.config.settings import Settings; s = Settings(); print(s.data.raw_dir)" succeeds</verify>
  <done>Settings load from env; .env.example documents required vars</done>
</task>

<task type="auto">
  <name>Task 3: Create subreddit config YAML</name>
  <files>config/subreddits.yaml</files>
  <action>
Create config/subreddits.yaml with equity_subreddits and crypto_subreddits lists per PRD.md FR-ING-01. Default equity: wallstreetbets, stocks, investing, options, stockmarket. Default crypto: cryptocurrency, bitcoin, ethereum, CryptoMarkets. Use standard YAML format. This file will be loaded by ingestion code.
  </action>
  <verify>python -c "import yaml; d=yaml.safe_load(open('config/subreddits.yaml')); assert 'equity_subreddits' in d and 'crypto_subreddits' in d" succeeds (add PyYAML to dev deps if needed, or use a minimal loader)</verify>
  <done>config/subreddits.yaml exists with equity and crypto subreddit lists</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pip install -e . succeeds
- [ ] python -c "import imst_quant; from imst_quant.config.settings import Settings; Settings()" succeeds
- [ ] All directories in data/raw and data/bronze exist
- [ ] .env.example is in .gitignore for .env (keep .env.example committed)
</verification>

<success_criteria>

- All tasks completed
- Repository structure matches RESEARCH.md specification
- Pydantic Settings load without errors
- No hardcoded credentials or paths
  </success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-data-ingestion-infrastructure/01-01-SUMMARY.md` per summary template.
</output>
